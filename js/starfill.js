/* starfill.js writes a number of blue stars, which is the lone argument (up to 5) and fills the rest with gray stars.
This is used to create a rating system, currently implemented in Chalrie Butler's online resume.

By Woody Butler
Decemeber 2014
*/

function starfill(skill) {
	document.getElementbyId
	if (skill > 5)
		skill =5;
	for (i=0; i<skill; i++){

	}

}